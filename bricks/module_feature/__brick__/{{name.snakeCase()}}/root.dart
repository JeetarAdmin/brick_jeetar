import 'package:flutter/widgets.dart';

class {{name.pascalCase()}}RootWidget extends StatelessWidget {
  const {{name.pascalCase()}}RootWidget({
     Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
